# setup with setuptools

from setuptools import setup, find_packages

setup(
    name='webshop',
    version='0.0.1',
    description='Webshop environment for OpenAI Gym',
    url="",
    author="",

    packages=find_packages(),
)